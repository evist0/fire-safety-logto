{
  "name": "root",
  "private": true,
  "scripts": {
    "build": "pnpm -r build",
    "lint": "pnpm -r lint",
    "test": "pnpm -r test"
  },
  "workspaces": {
    "packages": [
      "connectors/*"
    ]
  }
}
